node_modules
out
out2
dist
# build output is in dist/ and out/ — keep build/ tracked for electron-builder resources
*.log
.DS_Store

# Test coverage
coverage/

# Local env / secrets — never commit real keys (CI injects them from GitHub secrets)
.env
.env.*
!.env.example
